all:
  hosts:
    myrunner:
      ansible_host: ${{ matrix.os }}-runner-${{ matrix.runner_number }}.github.com
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ${{ secrets.SSH_PRIVATE_KEY }}
