name: workflow C

on: [workflow_call,workflow_dispatch]

jobs:
  workflow-C-Run:
    runs-on: ubuntu-latest
    steps:
      - name: Run workflow statement
        run: |
          echo "This is an example of workflow C failure"
          exit 1
